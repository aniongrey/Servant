# 2026-09-24

## 首次推送到新远端 aniongrey/Servant

- 仓库此前无任何提交：727 个文件全部处于 staged（`A`）状态，工作区无未跟踪残留，`.gitignore` 已写好（排除 `dist/`、`dist-fast/`、`node_modules/`、`.local/`、`.venv-memory/`、`/models/`、`provisioning-state.json` 等）。
- 补齐推送所需的两件事：
  - 提交身份：从 `codex-list` 仓库沿用 `user.name=Codex` / `user.email=codex@local`（本机 global/system 都没有 identity，不配会 commit 失败）。
  - 推送凭据：本机 **Git Credential Manager（GCM 2.9.0）** 里已存有 `github.com` 条目（账号 `cenlinghui`，token 前缀 `gho_`），可直接复用，**无需重新登录**。
- `.git/config` 的凭据链写法：`[credential] helper =`（空值，用于清掉 system 级 `helper-selector`）+ `helper = manager`。system 的 `helper-selector` 是交互式的，保留它会阻塞非交互推送。
- 结果：`git commit c697187 "chore: import Shiro desktop companion codebase"` → `git push -u origin main` 成功（14s，走 `http.proxy=127.0.0.1:7890`）。远端 `main` = `c697187`，tracking 已建立。
- 权限确认：GitHub API 查 `aniongrey/Servant` 返回 `permissions.push=true / admin=true`（public 空仓库，凭据账号是协作者）。
- 注意：`.workbuddy/memory/MEMORY.md` 也在首次提交中被推上去了（仓库是 public）。

## VRM 忽略规则：只跟踪 TestModel（提交 e442e5f）

- 变更点：`.gitignore` 的 `public/assets/character/` 段里，**删掉整目录排除行 `public/assets/character/`**，换成按文件名的白名单写法：
  ```
  public/assets/character/*.vrm
  !public/assets/character/TestModel.vrm
  ```
  整目录排除会把 `TestModel.vrm` 一起吞掉，达不到「只留一个中性模型」的目的；`!` 否定规则必须写在忽略规则之后才生效。
- 验证：`git check-ignore -q` 逐个判定 + `git ls-files --others --ignored --exclude-standard` —— 8 个角色 vrm 被忽略，`TestModel.vrm`（11MB）已入库并推送。
- **遗留影响**：`src/character/vrm/assets/default-character.json` 的 `vrmUrl` 仍指向 `可莉.vrm`，`src/character/micro-dynamics/micro-dynamics.json` 指向 `coco小熊.vrm` —— 二者现在都不入库，新克隆者只有 TestModel，这两处需要改或由用户自备模型。
- `public/assets/character/绑骨解决方案.txt` 仍未跟踪也未忽略（会出现在 `git status`）。
- 环境注意：本仓库可能被用户用其它工具**并行提交**（本次遇到操作中途 HEAD 从 `c697187` 变成外部提交 `7178880 rdme update`，导致一次 commit 落空）。提交前先看 `git log --oneline -1` 确认基线。

